{
  "name": "huddol",
  "version": "0.0.4",
  "description": "Run your own organization of agents.",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "dev:app": "pnpm --dir app exec tauri dev",
    "dev:web": "pnpm build:webui && uv run --project core python -m huddol --webui-dir webui/dist",
    "start:web": "node scripts/start-web.mjs",
    "build": "pnpm build:cli && pnpm build:app --debug --no-bundle --ci",
    "build:webui": "pnpm --dir webui build",
    "build:core": "node scripts/build-core.mjs",
    "build:python": "node scripts/build-python.mjs",
    "build:cli": "cargo build -p huddol-cli --bin huddol-cli --locked",
    "build:app": "pnpm --dir app exec tauri build",
    "format": "pnpm format:config && pnpm format:webui && pnpm format:core && pnpm format:rust",
    "format:check": "pnpm format:config:check && pnpm format:webui:check && pnpm format:core:check && pnpm format:rust:check",
    "format:config": "pnpm format:toml && pnpm format:yaml",
    "format:config:check": "pnpm format:toml:check && pnpm format:yaml:check",
    "format:toml": "tombi format --offline",
    "format:toml:check": "tombi format --offline --check",
    "format:yaml": "node scripts/format-yaml.mjs --write",
    "format:yaml:check": "node scripts/format-yaml.mjs --check",
    "format:webui": "pnpm exec biome format --write .",
    "format:webui:check": "pnpm exec biome format .",
    "format:core": "uv run --directory core ruff format . ../scripts",
    "format:core:check": "uv run --directory core ruff format --check . ../scripts",
    "format:rust": "cargo fmt",
    "format:rust:check": "cargo fmt --check",
    "check": "pnpm check:config && pnpm check:webui && pnpm check:core && pnpm check:rust",
    "check:config": "tombi lint && actionlint && pnpm exec lefthook validate",
    "check:webui": "pnpm exec biome check --formatter-enabled=false . && pnpm --dir webui check",
    "check:core": "uv run --directory core ruff check . ../scripts && uv run --directory core mypy src/huddol && uv run --directory core lint-imports",
    "check:rust": "node scripts/cargo.mjs check && node scripts/cargo.mjs clippy --all-targets -- -D warnings",
    "test": "pnpm test:webui && pnpm test:core && pnpm test:rust",
    "test:webui": "pnpm --dir webui test",
    "test:core": "uv run --directory core pytest -W error",
    "test:rust": "node scripts/cargo.mjs test",
    "icon": "pnpm --dir app exec tauri icon ../assets/icon.json",
    "release": "cargo release",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "lefthook": "^2.1.10",
    "prettier": "3.9.6",
    "tombi": "1.5.2"
  }
}
