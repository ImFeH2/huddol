{
  "name": "huddol-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Run your own organization of agents.",
  "packageManager": "pnpm@12.3.4",
  "type": "module",
  "scripts": {
    "build:app": "node scripts/run-app.mjs build",
    "build:core": "node scripts/build-core.mjs",
    "build:frontend": "pnpm --dir app build:frontend",
    "check": "pnpm check:config && pnpm check:web && pnpm check:core && pnpm check:rust",
    "check:config": "actionlint && pnpm exec lefthook validate",
    "check:core": "uv run --directory core ruff check . && uv run --directory core ruff format --check . && uv run --directory core mypy src/huddol && uv run --directory core lint-imports",
    "check:rust": "cargo fmt --check && node scripts/cargo.mjs check && node scripts/cargo.mjs clippy --all-targets -- -D warnings",
    "check:web": "pnpm exec biome check . && pnpm --dir app typecheck",
    "dev": "node scripts/run-app.mjs dev",
    "dev:release": "node scripts/run-app.mjs dev --release",
    "format": "pnpm exec biome format --write . && uv run --directory core ruff format . && cargo fmt",
    "icon": "pnpm --dir app exec tauri icon ../assets/icon.json",
    "release": "release-it",
    "test": "pnpm test:web && pnpm test:core && pnpm test:rust",
    "test:core": "uv run --directory core pytest -W error",
    "test:rust": "node scripts/cargo.mjs test",
    "test:web": "pnpm --dir app test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@release-it/bumper": "^8.0.0",
    "lefthook": "^2.1.10",
    "release-it": "^21.0.0"
  }
}
